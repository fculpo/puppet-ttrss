[Unit]
Description=ttrss_backend
After=network.target mysql.service postgresql.service

[Service]
User=<% @webserver_user %>
ExecStart=<%= @webroot -%>/<%= @dirname -%>/update_daemon2.php

[Install]
WantedBy=multi-user.target